{
  "id": "ds_send_sequence",
  "name": "전송차수 채번",
  "type": "DB_QUERY",
  "query": "SELECT NVL(MAX(send_seq),0)+1\n  AS seq_no\nFROM tb_cms_send_log\nWHERE inst_id = :instId\n  AND file_type = 'EB13'\n  AND send_date = :sendDate",
  "params": [
    { "name": "instId", "source": "context.institutionId" },
    { "name": "sendDate", "source": "context.sendDate" }
  ],
  "stub": {
    "scenarios": {
      "default": { "seq_no": 1 },
      "second_send": { "seq_no": 2 }
    }
  }
}
